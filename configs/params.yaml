training:
  model: yolov8n.pt
  epochs: 100
  batch_size: 16
  image_size: 640
  patience: 20
  device: cpu

prediction:
  confidence_threshold: 0.25

evaluation:
  iou_threshold: 0.5

validation:
  min_map50: 0.80