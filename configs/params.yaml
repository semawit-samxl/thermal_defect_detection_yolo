training:
  model: yolov8n.pt
  epochs: 200
  batch_size: 16
  image_size: 640
  patience: 20
  device: 0

prediction:
  model_path: runs/detect/train/weights/best.pt
  confidence_threshold: 0.25

evaluation:
  iou_threshold: 0.5

validation:
  min_map50: 0.80